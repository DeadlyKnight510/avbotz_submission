#include <cmath>
#include <math.h>
#include <iostream>

double c = 0.0;
double b = 2.63474*pow(10, -4);
double a = 7.07023*pow(10, -4);

// A(.5,0)
// B(-.5,0)
// C(0,1)

double substituteX;
double substituteY;

double x,y;

int main()
{
//	x = pow(a,2)-pow(b,2);
	x=b*b;
	std::cout << x<< "\n";
	return 0;
}
